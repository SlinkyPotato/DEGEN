{
  "$jsonSchema": {
    "bsonType": "object",
    "required": [
      "serverId",
      "name",
      "isDEGENActive"
    ],
    "properties": {
      "serverId": {
        "bsonType": ["string"],
        "description": "Discord ID of the server."
      },
      "name": {
        "bsonType": ["string"],
        "description": "Discord server name"
      },
      "isDegenActive": {
        "bsonType": ["bool"],
        "description": "Flag that is true if the discord server has DEGEN active"
      }
    }
  }
}
{
  "$jsonSchema": {
    "bsonType": "object",
    "required": [
      "twitterId",
      "twitterSpaceId",
      "startDate",
      "isEligible"
    ],
    "properties": {
      "twitterId": {
        "bsonType": "string",
        "description": "The twitter ID of the user."
      },
      "twitterSpaceId": {
        "bsonType": "string",
        "description": "The twitter space ID of the event."
      },
      "startDate": {
        "bsonType": ["string", "null"],
        "description": "ISO8601 of when the user entered the event."
      },
      "isEligible": {
        "bsonType": "string",
        "description": "Flag that is true if the attendee is eligible for poap claim."
      },
      "endDate": {
        "bsonType": ["string", "null"],
        "description": "ISO8601 of when the user left the event."
      }
    }
  }
}
{
  "$jsonSchema": {
    "bsonType": "object",
    "required": [
      "twitterId",
      "twitterSpaceId",
      "tweetId",
      "dateOfTweet"
    ],
    "properties": {
      "twitterId": {
        "bsonType": "string",
        "description": "The twitter ID of the user."
      },
      "twitterSpaceId": {
        "bsonType": "string",
        "description": "The twitter space ID of the event."
      },
      "tweetId": {
        "bsonType": "string",
        "description": "Tweet ID for POAP eligibility"
      },
      "dateOfTweet": {
        "bsonType": ["string", "null"],
        "description": "ISO8601 of when the tweet was sent"
      }
    }
  }
}
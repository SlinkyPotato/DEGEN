{
  "$jsonSchema": {
    "bsonType": "object",
    "required": [
      "discordObjectId",
      "discordObjectName",
      "discordServerId",
      "discordServerName",
      "objectType"
    ],
    "properties": {
      "discordObjectId": {
        "bsonType": "string",
        "description": "Discord Id of the user that is configured to use this command."
      },
      "discordObjectName": {
        "bsonType": "string",
        "description": "Discord handle of the user that is configured to use this command."
      },
      "discordServerId": {
        "bsonType": "string",
        "description": "Identifier for the discord guild"
      },
      "discordServerName": {
        "bsonType": "string",
        "description": "The name for the discord guild name"
      },
      "objectType": {
        "bsonType": "string",
        "description": "Can be either ROLE or USER"
      }
    }
  }
}